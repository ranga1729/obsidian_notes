
1. Find providers' API, their models, capabilties, features, limitations.
```text
There several AI API service providers like Google, OpenAI. each of these providers have different APIs like OpenAI has realtime API, respone api,...etc and google has live API, .... . and each of these providers and their apis support different AI models. those models, APIs has different capabilities and features like live API use Websocket to reduce the response time and able to build a realtime chat behavior. some models/APIs support automatic voice detection, some of them can execute tools, some of them support voice inputs, some of them support text responses only, some of them support voice responses. like that there are lots of variations in providers, their APIs and their models. I'm building an platform where my users can choose a provider, an api and a support model. then they can see what are the capabilities, features and limitation of their selection combination. Then I provide a websocket connection to users. by connecting to that webscoekt, users can interact with the ai model they selected. if they support text input I will give my users a JSON standard whcih they can user to interact with my websocket. example, for text inputs "{ "type": "text_input", "value": "Hello, world"}". like this, I will maintain my own standard for taking inputs and giving response. becuase I'm using an abstraction layer in my api service so my users can use a one standard to communication with different ai api providers(openai, google) and their different apis, and different models without relying on one provider and use thier standard. my users will be the ones who want to build their voice agents, chat applications, chatbot features. for this platform, I need to know all of the AI APIs I can use (by google and openai), their supporting models for each api, their special features, capabilities, limitations. for an example, google live api. provider: google, api: google live api, features: "https://ai.google.dev/gemini-api/docs/live-api#key-features, webhook support, sdk support". capabilities: multi modal support,... Limitations: text only mode(response_modality="TEXT") has been deprecated, models: google live preview(or something similar name).... I need do this type of a thorough analysis.
```

```text
Ok here. issue 1 - I acknoeledged the mistakes and thanks for correcting me. Issue 2 - here is the way I want to correct this. for the RAG, I was thinking to inspect the tool call request coming form the LLM provider to my backend. if the tool name is "search_internal_knowledge", i will not forward this to the user, instead I will intercept the tool call request, do the vector DB querying, and send the result back to the LLM. since this is the only tool I'm going to internally define, I think intercepting this particular tool is ok(but if you have a better solution, suggest me. but at the end what should be done is this. this "search_internal_knowledge" should be resolved internally without formwarding it to the user). and remove that "tool_call_intercept" event. instead I think it's better to have a server side event called "access_internal_knowledge", so if we maintain a log of server-side event, we can log that, and may be we can send it to the user so user know now their query is acessing the internal knowledge. Issue 3 - Yes I accept tha fact that we must remove this tool_call.request and tool_call.response columns from tables since they store user's messages and responses. and I need a better way to store tools in the backend. these are my concerns, since now I'm considering to allow users to add mcp servers, there should be a table to store them that linked with a project_id. also there should be a table which includes all the tools that linked with the project_id as well. but I heared that some mcp server have multiple endpoint urls for the same tool, so there should be a way to handle that as well. because we can use it as a fallback mechanism. if the first endpoint failed/timeout or error occured, we must try the second url. and if a particuar tool is coming from a mcp server, there should be a link between them a well. Issue 4 - yes. store the key vault secret reference name. Issue 5 - Completley agree with you. we must add a boolean called rag_enabled into the Projectconfig. If users has upload any document, that's the time we must enable it. so the adapter won't have to use that tool in unnecessary times. I need a proper mechanism to handle that in the architecture. also, since we allow users to uplaod documents, I was thinking to use Azure Blob storage. so we can use the Azure AI search(as the vector DB) with Blob service. may be We will need another table to track the links of those docuemnts that our user uploaded for a project. Issue 6 & Issue 7 - yes. I was added later. I need to mention it in the SRS. I guess the Azure AI search handle the entire pipeline of this vectorization including the embedding of those documents. and if a user delete the project, they must be deleted as well. from the block storage and the azure ai search as well. Supported file types will be PDF, DOCX, TEXT, Markdown. Issue 8 - Yes it should be acknowledged at the SRS. also if the user has enabled the RAG, we will communicate it in the front-end. and I think having that "rag_timeout_ms" at the project level is also a better idea. Issue 9 - acknoelwdged. Issue 10 - acknowledged. this must be mentioned in the SRS clearly. Issue 11 - I was thinking to give platform users a button so they can manually trigger the re-discovery of tools from connection mcp servers. in-addition, I think it's good to have a automatic re-discover mechanism at the prject level using the per-project connection pool option you mentioned. Issue 12 - yes. agree. must be removed. already mentioned above. Issue 13 - yes. this must be applied too. Issue 14 - yes. butI have added any table including the plan details(like free, pro, enterprises). and I'm agree on the fact that the prompr version count must be depend on the subscription plan. So we will need a new table called "subscription_plans". can you please suggest me a pricing strategy for this platforms ? Issue 15 - yes, since we allow multiple prompts, i think we might need to have a separate table for system_prompts. and that table has a FK relation to a project_config. and the table has a attribute to indicate which prompt must be active at the time like "is_active" or something. so when we imporing the project config, we can grab all the prompts including the is_active being true on the active prompt. Issue 16 - agree. Issue 17 - when I'm saying prompt_versioning, I mean I'm allowing the user to have multiple system prompts. not to track versions of the same prompt. since the users is allowed to have multiple prompts, we must keep track what is the active one. and my future plan was to let the users do A/B testing while keeping musltiple propmpts active. example: two system prompts are active. both of them are active for the A/B testing. half of the new webscoket connections will load the "prompt_a" and the other half will load "prompt_b". and we will let users keep it for a while. then we can show some matrices of hwo they performed. but this feature is not yet confirmed and have no idea what matrics we should show as the A/B testing reults. since that, keep this away from the current implementation. for now lets allow users to have x number of system propmpt(x must be depend on the payment plan) and one of them must be active at a time. platform users can change the active propmpt(all prompts must have a label to easily identify). then the next websocket open will load this new activated prompt. Issue 18 - no idea what's this "traffic_weight" means. but don't apply changes on that A/B testing feature. we will discuss it later. 
```

2. Edits for text response mode
```text
ok, here are the changes I'm going to do, I will edit the project config to have a new attribute called "text_response_mode" and it can be either "streaming" for incremental streaming text responses and "complete" for concatenated full text response without chunks being arrive to the client. 
Assume that all LLM provider will send us text/voice responses in a streaming style, chunk by chunk and all llm providers send some indications to mark the end of the streaming. But expect no LLM provider will send us the concatenated text response at the end of the response stream. 
so no point of buffering llm responses in the adapter layers waiting for a TextDone event with a full text response in it. 

So I want to make the adapter layer a single responsible and reduce any latency/delay from that. Adpater layer's main responsibility must be to have the LLM apis integration and sit in between the protocol handler and the LLM provider. it will recieve responses and convert them into LLMEvents and send to the protocol handler. and protocol handler will call adapter's right method when it need to send a message to the LLM api. so the adpater will act as a converter. so no buffering in the adapter layer, it just immdediately forward responses it received to the protocol handler as a LLMEvent and immedately take messages coming from the protocol handler to the LLM api. 

And I'm going to add a new components called TextAccumulator. this component's sole responsibility is to run in the background and accumulate text chunks into a full complete text. this resided at the same level as the protocol handler. as the protocol handler read LLMEvent from the adapter, this components will also read them. and it maintains a buffer locally. it will add all those TEXT events from the adpater to it;s buffer and wait for the stream end marker event. once it receives, it will concatenate all buffered text and out it into the Redis for conversation history in the correct session. 

Also a similar job will be done by the Protocol handler as well. If the user's preferred text response mode is streaming, it will read the LLMevent fromt he apdater, conver them to the platform's unified protocol(JSON response styles) and immediately stream them to the user. TurnComplte event also will be streamed in the same manner.

But if the user's perefferes text response mode is "Complete", the protocol hanlder will buffer the text responses in the protocol hanlder and concatenate texts until the TurnComplete marker arrives. then the complete concatenated text will be send to the user as a whole text. 

The text accumulator will work parallel to the protocol hanlder for maintaing the conversation history. and the LLM providers will send transcripts of the audio queries as well. so the text accumulator must listen to all of them and the it must record history in turn wise. 
Like,
{ "query": <question asked by the users>, "answer": <response by the LLM>}
"query" may be got from the direct text query send by the users or got fromt the transcription if the user gave the query as in an audio format. and the "answer" is got from the direct LLM's text responses or the transcription of the LLM's response if the response was in audio format.

Also the protocol handler will intercept RAG tool calls since the RAG is implemented locally. RAG calls will not be sent to the users, but the users will recveive an indication that the model is now searching for the internal knowledge(only if the rag is enabled). 

These will be the LLMEvents emit by the adpater layer. 

// For text responses
TextDeltaEvent { Text: "..." }  // Per chunk, immediately
TextDeltaEvent { Text: "..." }  // Per chunk, immediately
TurnCompleteEvent { }           // When provider's end-marker arrives

// For audio responses
AudioDeltaEvent { Audio: "...", SampleRate: 24000 }  // Per chunk
AudioDeltaEvent { Audio: "...", SampleRate: 24000 }  // Per chunk
TurnCompleteEvent { }                                 // When end-marker arrives

// For transcriptions (independent)
TranscriptEvent { Role: "user", Text: "..." }       // May arrive async
TranscriptEvent { Role: "assistant", Text: "..." }  // May arrive async

// For tool calls (provider-native)
ToolCallEvent { ToolCallId: "...", Name: "...", Arguments: {...} }

// Session lifecycle
SessionStartedEvent { }
SessionEndedEvent { }

Think like a Senior sottware architect. Tell me if this is ok. any mistake I have done or any optimization can be done to these changes ? 
```

```
I approve the idea of having Event dispatcher. and there should be a way for TextAccumulator to get other data well. may be we need to chnage the architecture or find another better solution for this. and usage data must also tracked. and session start event must be send after making sure the both sides connection are establoshed and the session ended event must be send if the provder side connection gets closed and yes it will be send by some other component. tool result processed event won't be necessary. it can be removed. and think audio data must be passed to the user form the adapter and in the other direction as well without any blocking. any text buffer to concatenate text must only concatenate texts and audio must be passed through. and audio data must be inteferred by the tool calls as well. and yes TextAccumulator maintains a per-turn state object that is flushed only at TurnCompleteEvent, regardless of transcript arrival order. and yes, history format must be compatible with the adapter but it must include full texts, not partials. for the response api, it must also maintain a separate conversation histroy(this is not the same thing as response id chaining, it must be a different thing). the reason why I'm trying to keep the history is I will introduce a provider fallback method as well. like it the provider stopped due to an error, my platform must be able to create a new connection with the provider (clienbt side connection will be continue) and the conversation history will be feed as the context so my users can keep the conversation without any interruption. and another concern is sometimes token usage data are in the LLM's response it self as I remember(correct me if im wrong). so our usage accumulator must be able to access those llm responses(like from the event dispatcher) or the protocol hanlder must tell those details to the usage accumulator. by considering all these concenrs, tell me hwo should I change the architecture of my plafrom.
```