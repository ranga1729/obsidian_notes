NextJS is a React Framework. It includes everything from react with some backend functionalities. 
It allows you to handle,
1. Server-side rendering
2. Routing
3. API Calls
Next allows you to setup routing without extra libraries like React router.

#### Project structure
![[Pasted image 20250707153812.png]]
app : contains your pages and global CSS files.
	![[Pasted image 20250707153904.png]]
	Here the `page.js` works as the main entry point. 
	Every folder you create inside the `app` folder corresponding to a route and every folder must have it's own `page.js` to work as the main rendering page of this specific route. 
	`layout.js` defines the layout. 