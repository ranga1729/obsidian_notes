A communication process must have three elements,
1. Source
2. Destination
3. Channel(media)

Communication follows a set of established rules called "protocols". Rules can be vary depend on the protocol.

A protocol is responsible for the following requirements,
1. An identified sender and receiver
2. Common language
3. Speed and timing
4. Confirmation or acknowledgement requirements

Protocols that are used in communication share above requirements and additionally include the following requirements,
1. Message encoding
2. Message formatting and encapsulating 
3. Message size
4. Message timing
5. Message delivery options

#### 1. Message Encoding
The process of converting information into another acceptable form for transmission. 
Decoding reverses this  process.
#### 2. Message Formatting and Encapsulation
Message formats depend on the type of message and the channel that is used to deliver the message. 
Ex: IPv4 packet format
![[Pasted image 20260516075845.png]]
#### 3. Message Size
when a message is being sent through a channel, its necessary to break it down into smaller pieces. The host will break the long message into individual pieces that meet both the minimum and maximum size requirements. 
The long message will be sent in separate frames, with each frame containing a pieces of the original message. 
#### 4. Message Timing
Timing includes,
- Flow control - Rate of data transmission and speed.
- Response timeout - Ho long a device waits for a reply.
- Access method - Determines when someone can send a message. 
	Some protocols are proactive and attempt to prevent collision and others are reactive by establishing a recovery method after the collision occurs. 
#### 5. Message Delivery Option
Types of delivery options,
- Unicast - one to one communication
- Multicast - one to many, typically not all
- Broadcast - one to all

ex: IPv4 uses broadcast. But IPv6 doesn't have that option instead IPv6 introduces "Anycast" as an additional delivery option for IPv6. 
![[Pasted image 20260516085522.png]]

